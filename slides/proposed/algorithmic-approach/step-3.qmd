## Step 3: Find the Desired Policy {.nav-proposed}

ETA finds the user's desired policy using APL. Specifically, utilizes a Gaussian Process to learn the user's utility $u(f)$ over $\mathcal{F}$,

explain POLAR?